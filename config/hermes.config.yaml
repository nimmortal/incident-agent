model:
  provider: "copilot"

mcp_servers:
  jira:
    url: "${JIRA_MCP_URL}"
    headers:
      Authorization: "Bearer ${JIRA_MCP_TOKEN}"
    enabled: true
    timeout: 120
    tools:
      resources: false
      prompts: false
