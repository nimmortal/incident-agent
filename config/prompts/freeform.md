{{featureContext}}

User request:
{{request}}

If your answer would include follow-up evidence recommendations and the relevant configured source can be checked read-only with the available context, do the next bounded check now instead of only recommending it. Leave follow-up work for later only when blocked, and state the blocker.
